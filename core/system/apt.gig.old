#!/bin/bash

#TODO, quiet starts, maybe handling target release? proposals?

# enable only if you have apt based distro
# if ! _:ginger_bin apt ; then return 1; fi


# sysupgrade()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get upgrade;
# }
# sysdupgrade()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get dist-upgrade;
# }
# sysupdate()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get update;
# }
# sysget()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get install $@;
# }
# sysrm()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get remove $@;
# }
# syspurge()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get purge $@;
# }
# sysclean()
# {
# 	if ! _:gassert_is_sudo ; then _:gt_error_n ' seems that you dont have sudo access' ; return 1; fi
# 	sudo apt-get autoremove;
# 	sudo apt-get autoclean;
# 	sudo apt-get clean;
# }

# pkgnfo-versions()
# {
# 	if ! ginger_require apt-xapian-index ; then return 1; fi
# 	for each in $@ ; do
# 		axi-cache madison $each
# 		echo '--------------------------'
# 	done
# 	return 0
# }

# pkgnfo-info()
# {
# 	if ! ginger_require apt-xapian-index ; then return 1; fi
# 	if ! axi-cache show $1 ; then
# 		echo ' maybe you have entered wrong or incomplete name'
# 		echo ' find it by pkginfo-list and then try again '
# 	fi
# 	return 0

# }

# pkgnfo-list()
# {
# 	if ! ginger_require apt-xapian-index ; then return 1; fi
# 	axi-cache search $1
# 	return 0

# }
