#!/bin/bash

# simply execute setup scripts for all config dirs
# each dir should register here and provide setup script 

# grab currents scirpt's location
# todo: add helper function to ginger
# disabled in public release, if you want this feature, 
# or build something on top of that, please so uncomment it
MYDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# . ${MYDIR}/provision.logrotate.sh
# . ${MYDIR}/provision.cron.sh

